# Build output — regenerated by `node build.js`
dist/

# macOS
.DS_Store

node_modules/
.vercel

# Source photographs — large originals, kept local. The compressed
# selfie-*.jpg files next to them are what the build embeds.
art/image-*.png
art/hero-image.png
